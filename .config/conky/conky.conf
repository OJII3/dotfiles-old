-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'bottom_left',
    background = true,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = true,
    extra_newline = false,
    font = 'Orbitron:size=20',
    font0 = 'Orbitron:size=40',
    font1 = 'Orbitron:style=Bold:size=20',
    font2 = 'Orbitron:style=Bold:size=16',
    gap_x = 60,
    gap_y = 60,
    minimum_height = 5,
    minimum_width = 600,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_transparent = true,
    own_window_colour = '#ffffff99',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'left',
    use_xft = true,
}

conky.text = [[
${color}${font Orbitron:size=100}${time %k:%M}
#${voffset -300}
#${image /usr/share/archlinux/logos/archlinux-official-light.svg -p 0,100 -s 300x100}
${color grey}${font2}$color $sysname $kernel $machine
${color red}$hr${font}
${color grey}RAM Usage: $alignr $color $mem/$memmax - $memperc%
${membar 4}
${color grey}Swap Usage: $alignr $font $color $swap/$swapmax - $swapperc%
${swapbar 4}
${color grey}CPU Usage: $alignr $font $color $cpu%
${cpubar 4}
${color grey}File systems: $alignr $color${fs_used /}/${fs_size /}
 ${fs_bar 6 /}
${color grey}Networking:
Up:$color ${upspeed} ${color grey} - Down:$color ${downspeed}
$hr
${color grey}Name              $alignr CPU%   MEM%
${color lightgrey} ${top name 1} $alignr ${top cpu 1}  ${top mem 1}
${color lightgrey} ${top name 2} $alignr ${top cpu 2}  ${top mem 2}
${color lightgrey} ${top name 3} $alignr ${top cpu 3}  ${top mem 3}
${color lightgrey} ${top name 4} $alignr ${top cpu 4}  ${top mem 4}
]]
